<template>
  <div>
    <span>{{ review }}</span>
    <!-- <span class="font-bold">{{ review.title }}</span>
    <span class="font-bold">{{ review.description }}</span> -->
  </div>
</template>

<script>
export default {
  props: ["LandmarkDetail"],
};
</script>